meta {
  name: Request with Invalid JSON
  type: http
  seq: 2
}

post {
  url: {{base_url}}/data
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "test",
    "invalid": true,
  }
}
